.godot/
.import/
*.translation
.DS_Store
export.cfg
export_presets.cfg

# Rust / cargo build artifacts (the native GDExtension is built locally;
# only the source, Cargo.toml and Cargo.lock are tracked).
rust/target/
